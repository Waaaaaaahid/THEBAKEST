import { useEffect, useState } from 'react';
import { Link, NavLink, useLocation, useNavigate } from 'react-router-dom'; import { AnimatePresence, motion } from 'framer-motion'; import { Menu as MenuIcon, X, ShoppingBag, User as UserIcon, LogOut, Package, LayoutDashboard, ChevronDown } from 'lucide-react'; import { useCart } from '@/lib/cart'; import { useAuth } from '@/lib/auth'; import { useCafeSettings } from '@/lib/cafe-settings'; import { Logo } from './ui';
const navLinks=[{to:'/',label:'Home'},{to:'/menu',label:'Menu'},{to:'/about',label:'About'},{to:'/contact',label:'Contact'}];
export function Navbar(){const [scrolled,setScrolled]=useState(false),[mobileOpen,setMobileOpen]=useState(false),[accountOpen,setAccountOpen]=useState(false);const {count}=useCart();const {user,isAdmin,signOut}=useAuth();const {settings}=useCafeSettings();const location=useLocation();const navigate=useNavigate();useEffect(()=>{const onScroll=()=>setScrolled(window.scrollY>8);onScroll();window.addEventListener('scroll',onScroll);return()=>window.removeEventListener('scroll',onScroll)},[]);useEffect(()=>{setMobileOpen(false);setAccountOpen(false)},[location.pathname]);const handleSignOut=async()=>{await signOut();navigate('/')};const fullName=user?`${user.firstName} ${user.lastName}`.trim():'';return <header className={`sticky top-0 z-50 transition-all duration-300 ${scrolled?'bg-white/90 backdrop-blur-md shadow-soft':'bg-white/70 backdrop-blur-sm'}`}><nav className="container-bk flex h-16 items-center justify-between gap-4 lg:h-20"><Link to="/" className="flex min-w-0 items-center gap-2"><Logo/><span className="hidden max-w-[180px] truncate font-display text-sm font-semibold text-bakery-primary lg:block">{settings.restaurantName}</span></Link><div className="hidden items-center gap-1 lg:flex">{navLinks.map(l=><NavLink key={l.to} to={l.to} end={l.to==='/'} className={({isActive})=>`relative px-4 py-2 text-sm font-medium transition-colors ${isActive?'text-bakery-primary':'text-bakery-ink/70 hover:text-bakery-primary'}`}>{({isActive})=><><>{l.label}</>{isActive&&<motion.span layoutId="nav-underline" className="absolute inset-x-3 -bottom-0.5 h-0.5 rounded-full bg-bakery-primary"/>}</>}</NavLink>)}</div><div className="flex items-center gap-1.5 sm:gap-2"><Link to="/orders" className="hidden rounded-full p-2.5 text-bakery-ink/70 transition-colors hover:bg-bakery-sky hover:text-bakery-primary sm:inline-flex" aria-label="My Orders"><Package className="h-5 w-5"/></Link><Link to="/cart" className="relative rounded-full p-2.5 text-bakery-ink/70 transition-colors hover:bg-bakery-sky hover:text-bakery-primary" aria-label="Cart"><ShoppingBag className="h-5 w-5"/>{count>0&&<motion.span key={count} initial={{scale:0}} animate={{scale:1}} className="absolute -right-0.5 -top-0.5 flex h-5 min-w-[1.25rem] items-center justify-center rounded-full bg-bakery-primary px-1 text-[10px] font-bold text-white">{count}</motion.span>}</Link>{user?<div className="relative hidden sm:block"><button onClick={()=>setAccountOpen(o=>!o)} className="flex items-center gap-1.5 rounded-full border border-bakery-primary/15 bg-white px-3 py-2 text-sm font-medium text-bakery-ink"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-bakery-primary text-xs font-bold text-white">{fullName.charAt(0).toUpperCase()||'U'}</span><span className="max-w-[80px] truncate">{fullName||'Account'}</span><ChevronDown className={`h-4 w-4 transition-transform ${accountOpen?'rotate-180':''}`}/></button><AnimatePresence>{accountOpen&&<motion.div initial={{opacity:0,y:8}} animate={{opacity:1,y:0}} exit={{opacity:0,y:8}} className="absolute right-0 mt-2 w-56 overflow-hidden rounded-xl border border-bakery-primary/10 bg-white shadow-card"><div className="border-b border-bakery-primary/10 px-4 py-3"><p className="truncate text-sm font-semibold">{fullName}</p><p className="truncate text-xs text-bakery-ink/50">{user.role==='admin'?'Admin':'Customer'}</p></div><Link to="/profile" className="flex items-center gap-2.5 px-4 py-2.5 text-sm hover:bg-bakery-sky"><UserIcon className="h-4 w-4"/> My Profile</Link><Link to="/orders" className="flex items-center gap-2.5 px-4 py-2.5 text-sm hover:bg-bakery-sky"><Package className="h-4 w-4"/> My Orders</Link>{isAdmin&&<Link to="/admin" className="flex items-center gap-2.5 px-4 py-2.5 text-sm font-semibold text-bakery-primary hover:bg-bakery-sky"><LayoutDashboard className="h-4 w-4"/> Admin Panel</Link>}<button onClick={handleSignOut} className="flex w-full items-center gap-2.5 border-t border-bakery-primary/10 px-4 py-2.5 text-sm text-error"><LogOut className="h-4 w-4"/> Sign Out</button></motion.div>}</AnimatePresence></div>:<Link to="/login" className="btn-primary hidden sm:inline-flex !px-5 !py-2">Sign In</Link>}<button onClick={()=>setMobileOpen(o=>!o)} className="rounded-full p-2.5 text-bakery-ink lg:hidden" aria-label="Menu">{mobileOpen?<X className="h-5 w-5"/>:<MenuIcon className="h-5 w-5"/>}</button></div></nav><AnimatePresence>{mobileOpen&&<motion.div initial={{height:0,opacity:0}} animate={{height:'auto',opacity:1}} exit={{height:0,opacity:0}} className="overflow-hidden border-t border-bakery-primary/10 bg-white lg:hidden"><div className="container-bk flex flex-col gap-1 py-4">{navLinks.map(l=><NavLink key={l.to} to={l.to} end={l.to==='/' } className={({isActive})=>`rounded-xl px-4 py-3 text-sm font-medium ${isActive?'bg-bakery-sky text-bakery-primary':'text-bakery-ink/80'}`}>{l.label}</NavLink>)}<div className="my-2 h-px bg-bakery-primary/10"/>{user?<><Link to="/orders" className="flex items-center gap-2.5 rounded-xl px-4 py-3 text-sm"><Package className="h-4 w-4"/> My Orders</Link><Link to="/profile" className="flex items-center gap-2.5 rounded-xl px-4 py-3 text-sm"><UserIcon className="h-4 w-4"/> Profile</Link>{isAdmin&&<Link to="/admin" className="flex items-center gap-2.5 rounded-xl px-4 py-3 text-sm font-semibold text-bakery-primary"><LayoutDashboard className="h-4 w-4"/> Admin Panel</Link>}<button onClick={handleSignOut} className="flex items-center gap-2.5 rounded-xl px-4 py-3 text-sm text-error"><LogOut className="h-4 w-4"/> Sign Out</button></>:<Link to="/login" className="btn-primary mt-2 w-full">Sign In</Link>}</div></motion.div>}</AnimatePresence></header>}
